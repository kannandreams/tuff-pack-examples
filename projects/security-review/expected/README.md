# Expected outcome

`service.py` is one correct solution for comparison and automated verification. The baseline should report zero signals after it replaces the demo's starting file. A deeper review and tests are still required before treating equivalent production code as secure.
