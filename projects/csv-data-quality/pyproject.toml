[project]
name = "csv-data-quality-agent"
version = "0.1.0"
description = "A Tuff-packaged CSV data-quality agent demo"
requires-python = ">=3.12"
dependencies = [
  "openai-agents>=0.2.0",
  "rich>=14.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["runtime"]
