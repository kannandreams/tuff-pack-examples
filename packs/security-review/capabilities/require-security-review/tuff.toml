id = "require-security-review"
version = "1.0.0"
type = "hook"
description = "Rerun the deterministic security baseline before Claude finishes and block on signals."
files = ["gate.py", "README.md"]

[hook]
event = "stop"
command = "python3 .claude/hooks/require-security-review/gate.py"
working_directory = "."
