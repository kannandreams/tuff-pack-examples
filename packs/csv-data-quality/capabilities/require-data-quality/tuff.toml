id = "require-data-quality"
version = "1.0.0"
type = "hook"
description = "Rerun the CSV quality check before Claude finishes and block on findings."
files = ["gate.py", "README.md"]

[hook]
event = "stop"
command = "python3 .claude/hooks/require-data-quality/gate.py"
working_directory = "."
